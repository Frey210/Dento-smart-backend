name: Backend CI/CD

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: |
          python3 -m venv .venv
          . .venv/bin/activate
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
      - name: Run tests
        env:
          SKIP_PARTITIONING: "1"
          ENVIRONMENT: "test"
        run: |
          . .venv/bin/activate
          PYTHONPATH=. pytest -q

  deploy:
    runs-on: self-hosted
    needs: test
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Deploy on Raspberry Pi
        run: |
          set -e
          cd ~/dento-deploy/Dento-smart-backend
          git pull
          cd deploy/raspi
          docker-compose up -d --build backend
          docker-compose exec -T backend alembic upgrade head
